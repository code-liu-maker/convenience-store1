<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lhs.mapper.OrderItemMapper">

    <resultMap id="BaseResultMap" type="com.lhs.model.OrderItem">
        <id column="item_id" property="itemId"/>
        <result column="order_id" property="orderId"/>
        <result column="product_id" property="productId"/>
        <result column="product_name" property="productName"/>
        <result column="price" property="price"/>
        <result column="quantity" property="quantity"/>
        <result column="subtotal" property="subtotal"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="itemId">
        insert into tb_order_item(order_id, product_id, product_name, price, quantity, subtotal)
        values(#{orderId}, #{productId}, #{productName}, #{price}, #{quantity}, #{subtotal})
    </insert>

    <select id="findByOrderId" resultMap="BaseResultMap">
        select * from tb_order_item where order_id = #{orderId}
    </select>
</mapper>